/* Bridging header: PDX content C-ABI (the "Note" indicator column) + contribution
 * behavior C-ABI (edit/overview commands + openPath). Both on pc_common.h. */
#include "pdx.h"
#include "contrib.h"
