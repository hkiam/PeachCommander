/* CPFX.h - umbrella for the Peach Commander PFX (file-system) C ABI, imported by
 * PCPluginHost. The canonical SDK copy lives in Plugins/SDK/pfx.h; this is the
 * build copy (kept in sync; verified by Tools/check-sdk-headers.sh). */
#ifndef CPFX_UMBRELLA_H
#define CPFX_UMBRELLA_H
#include "pc_common.h"
#include "pfx.h"
#endif
