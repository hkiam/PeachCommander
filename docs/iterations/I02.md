# I02 — Navigation Complete, Sorting, Volumes, Command Enum

**Goal / Demo:** Navigate the whole machine by keyboard: sortable columns, volume
dropdown + free space, path bar with segment navigation, window title path,
Enter launches files. Command registry skeleton exists (all actions routed).

**Prerequisites:** I01. **Read first:** SPEC-002 §4-§6, SPEC-003 §2-§3,
menus-and-commands.md §2, performance.md (listing rules).

## Tasks

- [x] T01 Command registry skeleton (PCCommands): PCCommand/CommandDispatcher/
      CommandContext per SPEC-014 §1; migrate I01 actions (navigation, tab switch)
      to commands with TC names (cm_GoToParent, cm_OpenDirUnderCursor, …).
      **AC:** unit tests: dispatch, unknown command, id/name uniqueness assert.
- [ ] T02 Sorting: sort spec on DirectoryModel (name/ext/size/date, reverse,
      dirs-first, natural-number option, cached collation keys off-main),
      column-header click + Ctrl+F3..F6 commands, sort arrow UI, persisted per
      panel (in-memory until I05).
      **AC:** unit tests incl. umlauts/numbers ("file2" < "file10"); perf: re-sort
      100k < 150 ms (PCPerfTests scaffold created here with budgets.json).
- [ ] T03 `Tools/make-fixtures.sh` v1: tree-10k/tree-100k, unicode names, sparse
      big file; wire into tests (env `PC_FIXTURES_DIR` cache).
      **AC:** script idempotent, < 60 s for 100k, documented in test-strategy.
- [ ] T04 Volume support: volume model (mounted volumes, free/total, eject),
      drive combo per panel + free-space label, mount/unmount notifications
      update live; per-volume last-path memory; Alt+F1/Alt+F2 dropdown commands.
      **AC:** USB/DMG mount appears live (manual check noted); unit tests on model.
- [ ] T05 Path bar interactions: click segment → navigate; click empty → edit
      field (paste path, Enter); Ctrl+\ root, `~` home command.
      **AC:** demo flow asserts segment nav + typed path nav incl. `~`.
- [ ] T06 Enter semantics per SPEC-003 §3 rules 1-2,4,6 (launch via NSWorkspace,
      app bundles launch, packages config default); Ctrl+PgDn enters bundles.
      Window title = active path (F-012).
      **AC:** UI smoke: open TextEdit doc fixture (assert NSWorkspace call via
      injectable opener protocol — no real launch in tests).
- [ ] T07 Auto-refresh v1: FSEvents watcher on visible dirs, coalesced full
      re-list preserving cursor/scroll (incremental diff comes later).
      **AC:** integration test: touch/rm files in watched temp dir → model updates
      ≤ 500 ms; cursor kept on same name when possible.

## Exit checklist

- [ ] Budgets: 100k dir complete < 1 s confirmed on dev machine, recorded in STATE.md.
- [ ] Inventory updates (F-006, F-007, F-012, F-025..F-027, F-031, F-050..F-052, F-065, partial F-033, F-250 partial).
- [ ] STATE.md + tag `i02-done`.
