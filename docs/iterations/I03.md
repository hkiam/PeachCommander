# I03 — Selection Model, Icons, Status Bar

**Goal / Demo:** Full TC selection: Insert/Space/Num+/Num-/Num*, red-name marking,
status bar counts ("3 of 41 files, 2,1 M of 340 M"), file icons load lazily,
hidden-files toggle, dir size on Space, properties dialog (basic).

**Prerequisites:** I02. **Read first:** SPEC-003 (whole), SPEC-002 §5,§7,§8.

## Tasks

- [x] T01 Selection state machine (PCCommands `SelectionState` actor): marked-set +
      cursor ops per SPEC-003 §2/§4, `..` exclusion, selection history (Num/),
      operation-completion unmark hook (`unmarkCompleted`). Real invert + real
      selectAll + size aggregation. **AC met:** 88 unit tests. _(re-done 2026-07-21)_
- [x] T02 Key handling in PanelListView: correct numpad keycodes (69/78/67/75),
      Insert(114)/Space(49), Shift+arrow range toggle; **marked rows render red**
      via selection mirror; cursor frame via CursorRowView. Mark menu wired through
      CommandRegistry (AppMenu). _(re-done 2026-07-21)_
- [x] T03 Select/Unselect group dialogs (Num+/Num-): mask field w/ last-used
      history, files-only vs +dirs checkbox; applied to SelectionState via
      selectByMask/unselectByMask (WildcardMask). _(re-done 2026-07-21)_
- [x] T04 Status bar per panel: selected/total counts + sizes, TC wording & locale
      number formatting (`SelectionSummaryFormatter`, 18 tests); coalesced updates
      on cursor/selection change. _(re-done 2026-07-21)_
- [x] T05 Icon pipeline (`IconLoader`): async NSWorkspace icons off-main, UTType
      cache + `.app` LRU, placeholder-first, per-cell generation token
      (cancel-on-scroll), icon modes none/standard/all. _(re-done 2026-07-21)_
- [x] T06 Hidden files toggle (cm_SwitchHidSys, Ctrl+H), dir-size-on-Space
      (`DirectorySizeCalculator`: async walk + cache by path+mtime, symlink-safe,
      13 tests), Alt+Shift+Enter all-dir-sizes (bounded concurrency 4).
- [x] T07 Properties dialog v1 (Alt+Enter): name, kind, size, dates, perms string,
      symlink target — read-only (`FileProperties`/`FilePropertiesReader`, tests +
      `PropertiesDialog`); symlink shown with ↳ marker in list. _(2026-07-21)_

## Exit checklist

- [x] Inventory: F-009, F-028..F-030, F-036, F-038(column), F-054..F-058 done/partial.
- [x] Full unit suite green (159 tests); app launches; STATE.md updated.
      UI "demo flow" smoke is manual (interactive) — logic is unit-tested.
- [ ] tag `i03-done` (pending final manual demo pass).
