# I05 — Configuration System, Options Dialog, Persistence

**Goal / Demo:** Settings window (Cmd+,) with Layout/Display/Colors/Icons/
Operation/Copy pages working live; all settings + session (tabs come I06, so:
paths, sort, window frames, splitter, active panel) survive restart; brief view;
color-by-filetype; dark mode.

**Prerequisites:** I04. **Read first:** SPEC-013, docs/architecture/configuration.md,
SPEC-002 §3 (brief view).

## Tasks

- [x] T01 INI engine (PCFoundation): `INIDocument` (comment/order preserving),
      atomic debounced writes, `[meta] version`; `ConfigStore` actor (typed
      accessors + `changes()` notifications, corrupt-file recovery); `ConfigPaths`
      with `-ConfigRoot`/`PEACHCMD_CONFIG_ROOT` override. **AC met:** 31 tests
      (round-trip incl. comments, unicode, concurrent access, recovery).
- [x] T02 Options → ConfigStore (hidden, icon mode) + session.ini save/restore
      (window frame, splitter, per-panel path+sort, active panel) on change
      (coalesced) + quit (AppDelegate applicationShouldTerminate).
      **AC met:** verified session round-trip via seeded state files.
- [x] T03 Settings window shell (source-list page tree) + Operation and
      Copy/Delete-relevant options (confirm delete, delete-to-Trash default,
      select-dirs). _Layout chrome-toggle page: TODO._
- [~] T04 Display/Icons/Colors pages: icon mode, size format, brackets-around-dirs,
      dark/light appearance scheme (F-295) — live-applied + persisted.
      _Font/size picker, date-format, by-mask color rules: TODO (partial)._
- [ ] T05 Brief view (Ctrl+F1/F2) — TODO (needs a dedicated multi-column view).
- [~] T06 Mouse: NC mode implemented (left click = cursor, double-click = open,
      right-click = toggle mark); `Operation.MouseMode` config applied on restore.
      _Windows-mode multi-select + context menu + Settings popup: TODO._

## Exit checklist

- [~] Inventory: F-013/F-275/F-277/F-295 done; F-032/F-271/F-272 partial;
      F-002/F-021/F-059/F-270 TODO.
- [ ] STATE.md; tag `i05-done` (after T05/T06 + Layout page + color rules).
