# I06 — Tabs, Hotlist, History, Quick Search/Filter, Command Line

**Goal / Demo:** Daily-drivable: tabbed panels with lock, Ctrl+D hotlist,
Alt+Left/Right history, quick search jumps, Ctrl+S live filter, working command
line with cd/shell/completion. End of Phase A.

**Prerequisites:** I05. **Read first:** SPEC-001 §5, SPEC-003 §6-§7, SPEC-013 §2 Tabs.

## Tasks

- [~] T01 Tab model + UI: `PanelTabs`/`PanelTabState` (PCFoundation, 27 tests) +
      `TabBarView` (chip row, +, close ×, middle-click close). Ctrl+T/W, Ctrl+Tab
      cycling, new/close/select/lock; per-tab path+sort+cursor; session.ini
      persists all tabs. **AC met:** tab state-machine unit tests; multi-tab
      session round-trip verified (3-tab restore). _TODO: drag-reorder, context
      menu (duplicate/close-others), locked-tab navigation-opens-new-tab, Tabs
      options page, per-tab history._
- [ ] T02 History per tab (ring 50, persisted), Alt+Left/Right/Down commands +
      dropdown UI; cursor-position memory integrated (SPEC-003 §1).
      **AC:** unit tests incl. dedup of consecutive entries.
- [~] T03 Hotlist: `Hotlist` model (flat, hotlist.ini round-trip, 5 tests);
      Ctrl+D popup at the active panel with 1..9 shortcuts + "Add Current
      Directory"; entries navigate; persisted. _Nested submenus, remove/configure
      editor, dual-path entries: TODO._
- [~] T04 Quick filter (Ctrl+S): live substring/wildcard filter over visible
      entries with a red indicator; Esc clears, Enter freezes. _Quick-search jump
      mode + per-tab filter persistence: TODO._
- [x] T05 Command line (SPEC-001 §5): bottom field with cwd prompt + history;
      `ShellExecutor` (cd with ~/env/relative/.., else /bin/zsh -lc, 13 tests) +
      `PathCompleter` (Tab completion, 13 tests); output window; type-routing
      (letters focus the command line); Ctrl+Enter appends name, Ctrl+Shift+Enter
      appends full path. **AC met:** executor + completion unit tests.
- [ ] T06 Panel misc: Ctrl+Left/Right (F-063), Ctrl+U swap (F-064), Show-menu
      filter presets (Ctrl+F10..F12 + custom wildcard, "only selected"),
      Commands-menu standard dirs (Desktop/Home/Downloads).
      **AC:** command tests; inventory sweep for Phase A statuses.

## Exit checklist

- [ ] Phase A review: run full demo script (all iterations' flows); fix paper cuts
      found (file as tasks if > trivial, log in STATE.md).
- [ ] Inventory: F-005, F-008, F-035, F-060..F-064, F-066 done; Phase A P1s all done or logged.
- [ ] STATE.md (+ note "good checkpoint for user direction feedback"); tag `i06-done`.
