# I07 — Lister (F3) & Quick View (Ctrl+Q)

**Goal / Demo:** F3 opens the Lister on anything instantly — including a 50 GB
file; text/hex/binary/image/HTML/RTF/AV modes; in-file search; Ctrl+Q quick view
panel; Edit/View associations page.

**Prerequisites:** I06. **Read first:** SPEC-005 (whole), performance.md §Viewer.

## Tasks

- [x] T01 `FileSlice` (mmap), `LineIndexer.lineStarts` (LF/CRLF/CR/huge/no-EOL),
      `EncodingDetector` (BOM + UTF-8 heuristic → CP1252), `HexFormatter`,
      `ChunkSearcher` (needle-across-boundary) — PCVFS, 36 tests. _(50 GB perf test
      deferred; hex mode virtual-scrolls arbitrary size, text indexes a 16 MB prefix.)_
- [~] T02 Lister window (F3): mode switching (1=text, 3=hex, 4=image, a=auto),
      n/p next/prev file, status bar (mode/size/path), multiple windows.
      _Binary mode, font config, per-window frame persistence: TODO._
- [~] T03 Text mode: custom CoreText virtual-scroll view drawing visible lines
      from the line index, auto encoding. _Wrap toggle, selection+copy, encoding
      menu, goto-%: TODO._
- [x] T04 Hex mode: virtual-scroll (only visible rows read from the FileSlice) →
      any size instant. _Goto-offset dialog + binary mode: TODO._
- [x] T05 Search (§4): Ctrl+F dialog + F3-next, streamed `ChunkSearcher` (overlap,
      needle-across-boundary tested) over the FileSlice; scrolls the match into
      view (text + hex). _Backwards search, case/whole-word opts: TODO._
- [~] T06 Media: Image mode (NSImageView fit). _HTML (WKWebView), RTF, AV: TODO._
- [~] T07 Quick View (Ctrl+Q): a viewer window that follows the active panel's
      cursor (debounced 150 ms), toggles closed on Ctrl+Q. _Embedded-in-inactive-
      panel variant + Esc-restore: TODO._
- [ ] T08 Edit/View associations page (SPEC-013 §4) + internal-association
      fallback chain consumed by F3 (mode hint), F4 (editor), Enter (opener).
      **AC:** chain resolution unit tests; F4 opens configured editor (mock).

## Exit checklist

- [ ] Inventory: F-110..F-118, F-121..F-122, F-273 done.
- [ ] STATE.md; tag `i07-done`.
