# I08 — The VFS Refactor (Keystone)

**Goal / Demo:** No user-visible change — and that's the point: after refactoring
panels, operations, and Lister onto the VirtualFileSystem protocol, EVERYTHING
still works and all tests stay green. Unlocks archives, search-results panels,
FTP, and FS plugins.

**Prerequisites:** I07. **Read first:** SPEC-006 (whole), architecture.md §VFS.
Follow the §5 refactor order EXACTLY — it keeps the app shippable mid-refactor.

## Tasks

- [x] T01 PCVFS types already existed (protocol/VFSPath/capabilities/streams).
      Added `VFSError` (+ errno mapping) and **`LocalFS`** implementing the whole
      `VirtualFileSystem` protocol (streamed batched list, lstat stat with symlink
      classification, LocalReadStream/LocalWriteStream, mkdir/delete/rename/
      setAttributes/localFileIfAvailable; watch stub). **AC met:** reusable VFS
      conformance battery (`runVFSConformance`) run against LocalFS — 12 tests.
- [x] T02 `VFSRegistry` (scheme→fs) + `VFSNavigator` (stack of (fs,path) frames,
      push/pop nested fs, composed displayPath). 11 navigator tests.
- [ ] T03 Panels on VFS: DirectoryModel consumes VFS streams; watch capability
      gate; per-tab navigator; session format migration (add scheme, migrate v1).
      **AC:** all existing UI/unit tests green unchanged (that's the AC).
- [ ] T04 Operations on VFS: engine takes (srcFS,srcPaths)→(dstFS,dstPath);
      local fast paths behind capability checks; overwrite/append semantics via
      capabilities; F4-edit uses localFileIfAvailable + re-upload seam for real.
      **AC:** op integration suite green; new mock-VFS tests for stream copy,
      resume, error mapping.
- [ ] T05 Lister on VFS (FileSlice from VFSReadStream w/ progressive temp
      download when not mmap-able) + temp-extraction cache (Caches dir, LRU,
      session cleanup).
      **AC:** lister tests green with mock VFS; cache eviction test.
- [ ] T06 Purity gate: `Tools/check-vfs-purity.sh` (grep for FileManager/POSIX
      outside PCVFS allowlist) added to test.sh + CI.
      **AC:** gate passes; violations fixed or allowlisted with comment.

- [~] T03 Panels on VFS: `DirectoryModel.load(_:fs:)` consumes the VFS streaming
      list; `PanelController` now loads directories through `LocalFS` (the legacy
      `LocalDirectoryLister` path is retained only for tests). Verified: all tests
      green + app loads via VFS. _Per-tab VFSNavigator, watch-capability gate, and
      session scheme migration: TODO._
- [ ] T04 Operations on VFS (engine (srcFS,paths)→(dstFS,path); capability-gated
      fast paths). **NOT DONE — deferred.**
- [ ] T05 Lister on VFS (FileSlice from VFSReadStream + temp-extraction cache).
      **NOT DONE — deferred.**
- [ ] T06 Purity gate `Tools/check-vfs-purity.sh`. **NOT DONE — add after T03–T05
      migration removes FileManager/POSIX from PCApp/PCOperations.**

## Exit checklist

- [~] Additive keystone (T01/T02) done + conformance-tested; full suite green (333).
      Migration T03–T05 + purity gate T06 remain (behaviour-preserving core
      refactor — a separate focused pass, per SPEC-006 §5 order).
- [ ] STATE.md; tag `i08-done` (after migration).
