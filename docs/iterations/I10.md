# I10 — Find Files (Alt+F7)

**Goal / Demo:** TC search dialog: masks, filters, full-text with encodings/regex,
search inside archives, streaming results, "Feed to listbox" turns results into a
panel you can operate on. Saved search templates.

**Prerequisites:** I09. **Read first:** SPEC-008, performance.md §Search.

## Tasks

- [~] T01 `FileSearchEngine` (PCVFS actor): VFS walk (depth-limited, symlink-safe),
      name-mask (multi/OR) + size filters, cancellable `AsyncStream<SearchHit>`;
      11 tests (exact hit sets, depth, size, cancel). _Attr/date filters, worker
      pool, 100k perf test: TODO._
- [~] T02 Content matching: streams up to 16 MB via VFS read + ChunkSearcher,
      case-insensitive (ASCII) or exact. _Encoding matrix, whole-word, regex, hex,
      NOT-containing, perf budget: TODO._
- [~] T03 Find Files dialog (Alt+F7): name mask, start dir, find-text + case,
      depth; streaming results list; Start/Stop, View (F3), Feed to Listbox, Close.
      _General/Advanced/Load-Save tabs, edit/goto: TODO._
- [ ] T04 Archive search — TODO.
- [x] T05 `ResultsFS` + Feed-to-listbox: flat VFS over the real hit paths;
      navigating the active panel to it lists the results; ops resolve real files
      (copy-out/F3 work), `..` leaves; 12 tests. _Delete-from-results, title marker: TODO._
- [ ] T06 Templates — TODO.

## Exit checklist

- [ ] Inventory: F-150..F-156 done (F-157 waits for I16, F-158 for I17).
- [ ] STATE.md; tag `i10-done`.
