# I11 — Multi-Rename Tool (Ctrl+M)

**Goal / Demo:** Select 5000 files, Ctrl+M, build names from masks/counters/regex
with live preview, execute safely, undo. Presets + editor round-trip.

**Prerequisites:** I10 (uses queue engine + VFS). **Read first:** SPEC-009.

## Tasks

- [x] T01 Placeholder engine (SPEC-009 §2 table) incl. case-region modifiers,
      grapheme-correct ranges, counter spec parser; application order
      mask→replace→case.
      **AC:** exhaustive token unit tests (every table row + negatives).
      **Done:** `MultiRenameEngine` in PCFoundation (64 tests). Tokens [N]/[N#-#]/
      [E]/[C..]/[d][Y][M][D]/[P][G]/[U][L][F][n]/[[ ]]; gregorian/UTC dates.
- [x] T02 Search/replace incl. regex + `$1` substitution, multi via `|`,
      repeat-replace; collision detection (case-insensitive per volume).
      **AC:** matrix tests; collision cases flagged.
      **Done:** regex + literal search/replace, repeat-replace, case modes
      (unchanged/lower/upper/firstUpper/everyWord); `RenameResult.collides` set.
- [x] T03 Dialog UI: mask fields + insert buttons w/ history combos, counter
      block, case block, live preview grid (red invalid rows), Load/Save
      presets (renames.ini).
      **AC:** UI smoke; preview updates < 100 ms for 5k rows (debounced).
      **Done (partial):** `MultiRenameWindowController` — mask/search/replace
      fields, counter + case blocks, debounced live preview grid (invalid/collide
      rows in red). **Deferred:** insert-token buttons, history combos, Load/Save
      presets (F-176).
- [x] T04 Execution as queue op: two-phase temp-rename for intersecting sets,
      per-file errors (skip/abort), result log, Undo (reverse log) (F-175);
      works via VFS (zip/FTP capability-gated).
      **AC:** integration: 5k files < 5 s; undo restores exactly; zip rename test.
      **Done (partial):** two-phase temp-rename (`.pcren-<UUID>`) with per-file
      best-effort restore on failure, undo log, one-click Undo restores exactly.
      **Deferred:** run through TransferQueue op + VFS (zip/FTP); currently local
      FileManager only (Ctrl+M disabled inside archives).
- [ ] T05 Editor round-trip (§4).
      **AC:** tab-file re-import validation tests.

## Exit checklist

- [x] Inventory: F-170, F-171, F-173, F-175 done; F-174, F-176 deferred (F-172 in I16).
- [x] STATE.md; tag `i11-done`.

## Status (2026-07-22)

Core multi-rename shipped: engine (T01/T02) + dialog (T03) + local two-phase
execution & undo (T04), wired to Ctrl+M / Commands ▸ Multi-Rename Tool.
Deferred to later polish: token-insert buttons + history combos + preset
Load/Save (F-176), VFS/queue-backed execution (zip/FTP), editor round-trip
(T05, F-174).
