# I12 — Compare by Content & Synchronize Dirs

**Goal / Demo:** Editable side-by-side diff of two files; full Synchronize-dirs
dialog syncing two trees (incl. into a zip); panel compare-directories marking.

**Prerequisites:** I11. **Read first:** SPEC-010.

## Tasks

- [x] T01 Myers line-diff + intra-line refinement in PCFoundation; binary
      streamed compare (first-diff offsets, block map).
      **AC:** golden diff tests (fixtures incl. unicode, CRLF, huge lines).
      **Done:** `LineDiff` (Myers O(ND), delete+insert coalescing to .change,
      grapheme intra-line ranges, ignore-case/whitespace/line-ends; 26 tests).
      **Deferred:** binary streamed-compare offsets/block-map (hex diff).
- [x] T02 Diff window UI: synced panes, block coloring, next/prev, ignore-
      options, encoding pickers; selection rules (§1).
      **AC:** UI smoke on fixture pairs; option toggles re-diff.
      **Done:** `DiffWindowController` — single shared table (auto-synced rows),
      block coloring + intra-line highlight, next/prev diff, ignore-case /
      whitespace / line-ends toggles re-diff; §1 selection resolution in MWC.
      **Deferred:** per-side encoding pickers (UTF-8→Latin-1 auto-fallback only).
- [ ] T03 Edit mode: editable panes (NSTextView acceptable here — bounded by
      256 MB guard), copy-block buttons, save w/ encoding, undo, re-compare.
      **AC:** edit+save round trip preserving encoding/EOL.  (deferred)
- [x] T04 Compare-directories panel marking (§4, pure model op) + menu entries.
      **AC:** unit tests for mark rules matrix.
      **Done:** `DirCompareMarker` (files-only newer/only-here marking, tolerance,
      case-insensitive; 16 tests) + cm_CompareDirs / cm_CompareDirsWithSubdirs
      (recursive gather, top-level component marking) wired into the Mark menu.
- [x] T05 Sync engine: compare walk (streamed, size-date fast path, content
      option), state model (→/←/=/≠/orphans), asymmetric mode, empty-dir
      handling; sync execution composed onto op engine.
      **AC:** scenario matrix tests (SPEC-010 §5) incl. cancellation resume-
      safety; zip-as-right-side round trip.
      **Done:** `SyncModel.classify` pure state model (21 tests, full matrix) +
      `SyncScanner` (recursive local walk, size-date + chunked content compare) +
      `SyncExecutor` (dirs-first copy, deepest-first delete/trash).
      **Deferred:** run through TransferQueue/op-engine + cancellation UI; zip
      side (F-193 → I15). Scanner/executor lack unit tests (PCApp has no test target).
- [x] T06 Sync dialog UI: paths+mask+template, checkboxes, result grid w/
      colors + filter buttons + counts, row context menu (incl. open diff),
      confirm + progress, presets (syncs.ini).
      **AC:** UI flow: create drift in fixtures → sync → trees identical.
      **Done (partial):** `SyncWindowController` — path/mask fields, with-subdirs /
      by-content / ignore-date / asymmetric checkboxes, Compare→colored result
      grid with →/←/=/≠/delete glyphs + direction counts, confirm + Synchronize +
      re-scan. **Deferred:** search-template dropdown, filter buttons, row context
      menu (reverse/exclude/open-diff), progress bar, presets (F-194).

## Exit checklist

- [x] Inventory: F-191 done; F-190, F-192 partial; F-194 deferred (F-193 FTP side in I15).
- [x] STATE.md updated.

## Status (2026-07-22)

Core compare & sync shipped: Myers diff engine + read-only diff window (T01/T02),
compare-directories marking (T04), sync classification + local scanner/executor +
sync dialog (T05/T06). Deferred polish: diff edit-mode & hex/binary compare (T03),
per-side encoding pickers, sync filter buttons / row context menu / progress bar /
presets (F-194), queue-backed sync execution, and zip/FTP sync sides (F-193, I15).
