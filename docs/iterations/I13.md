# I13 — Button Bar, User Menu, Full Menus, Key Remapping

**Goal / Demo:** Customize a toolbar button running `em_MyBackup` with %-params;
Start menu with user commands; complete TC menu tree; remap any key on the Keys
options page; copy-names commands. End of Phase B.

**Prerequisites:** I12. **Read first:** SPEC-014, menus-and-commands.md.
When implementing T01, fetch the TC internal-commands list
(https://www.ghisler.ch/wiki/index.php/List_of_Internal_Commands) to align ids.

## Tasks

- [x] T01 Command registry completion: align names/ids with TC for all existing
      commands, add missing cm_ stubs (disabled w/ "not yet" toast + inventory
      ref), command browser dialog (F-255).
      **AC:** registry export dump test (ids stable snapshot committed).
      **Done (partial):** every cm_ referenced by a menu item or a shipped keyboard
      scheme is now registered — 3 as real aliases (cm_SelectAll/cm_RereadSource/
      cm_SwitchToTargetPanel) and 56 as `implemented:false` placeholders in
      `CommandStubs.swift` (a "not yet implemented" notice on direct invocation;
      auto-disabled in menus). `PCCommand.implemented` flag added; registry
      uniqueness + implemented-flag tests; `check-keymap.sh` now reports 0 pending.
      Command browser (F-255): standalone searchable `CommandBrowserWindowController`
      (name/category/description filter, greyed placeholders, double-click/Run →
      execute) via Configuration ▸ Command Browser…. **Deferred:** full TC numeric-id
      alignment (TOTALCMD.INC); wiring the browser as the "choose command" picker
      inside the toolbar/keymap/Start-menu editors.
- [x] T02 %-parameter expansion engine (%P %N %T %M %S %L %F %D %W, quoting,
      list-file generation) shared by user commands + buttons.
      **AC:** expansion matrix unit tests.
      **Done:** `ParamExpander` (PCFoundation, 20 tests): all tokens, quoting,
      per-kind cached list-file callback; used by the user-command runner.
- [x] T03 usercmd.ini (em_) model + Start menu rendering + editor dialog.
      **AC:** ini round-trip; em_ from cmdline works.
      **Done (partial):** `UserCommands` model (PCFoundation, 14 tests, ordered
      round-trip); Start menu populated from usercmd.ini and reloaded when the app
      reactivates; runner expands %-params and runs cm_/em_/programs (.app via
      `open -a`) through ShellExecutor. **Deferred:** rich add/edit/reorder editor
      dialog (currently "Change Start Menu…" opens usercmd.ini in the default
      editor + auto-reload). em_/cm_ from the command line now works (typing a bare
      cm_Name/em_Name in the command line executes it).
- [x] T04 Button bar: .bar parser/writer (TC-compatible), strip UI with overflow,
      icon loading (SF Symbol/png/icns/app), click/drag-file-onto-button, subbars,
      right-click edit, Customize dialog w/ command browser + icon picker;
      Shift+drag file → new button.
      **AC:** parse TC sample .bar fixtures; UI flow create+use button.
      **Done (partial):** `ButtonBar`/`BarButton` .bar parser+writer (PCFoundation,
      18 tests, TC round-trip); `ButtonBarView` strip at the top of the window
      (icon buttons from SF Symbols "sf:name"/file-app paths, text fallback,
      tooltips, click → cm_/em_/program/dir, right-click → Edit Button Bar…),
      a starter `default.bar` written on first run. **Deferred:** overflow chevron,
      drag & drop (file-onto-button / Shift-drag-to-create), subbars, and the
      Customize dialog with command browser + icon picker.
- [ ] T05 Full menu build-out per menus-and-commands.md §1 (all entries exist;
      not-yet features disabled linking inventory id), F-key bar live modifier
      relabeling (F-004 polish), Mark-menu copy-names set (F-092).
      **AC:** menu snapshot test (structure dump vs golden file).
      **Done (partial):** menu tree expanded to the TC structure — File (archive/
      attributes/comment/split-combine/checksum entries), Commands (CD Tree/Branch/
      Terminal/Volume/SysInfo/Run cmdline), new Net menu (FTP/mount), View (Brief/
      Full/Tree/Thumbnails + Sort By submenu + Refresh), plus Mark copy-names and
      Configuration/Start. Not-yet-implemented items are auto-disabled at startup
      (KeymapMenu.apply) and show their scheme accelerator. **Deferred:** exact TC
      item order/completeness (Comments, Print, Encode/Decode, Associate…), F-key
      bar live modifier relabel (F-004), and the menu snapshot test.
- [x] T06 Keymap engine + Keys options page: scheme files (tc-classic,
      macos-native per keyboard-shortcuts.md), user overrides, conflict UI,
      dynamic menu shortcut display, performKeyEquivalent routing.  DONE.
      **AC:** precedence unit tests; remap flow UI test; both scheme files
      complete against keyboard-shortcuts.md (checked by script comparing doc
      table ↔ scheme file — write `Tools/check-keymap.sh`).
      **Done (partial):** `Keymap`/`KeymapScheme`/`KeyChord` engine (PCFoundation,
      32 tests: layered user>scheme>builtin precedence, `""` suppression,
      chord(for:) display lookup, spec parse/round-trip, aliases). Two shipped
      scheme files `keymap-tc-classic.ini` (80) / `keymap-macos.ini` (81 bindings)
      transcribed from keyboard-shortcuts.md, bundled as resources and loaded at
      startup (Configuration/KeyScheme) with `keymap-user.ini` overrides.
      `Tools/check-keymap.sh` validates scheme names vs the registry (47 pending
      registration until their features land in T01). Dynamic menu shortcuts:
      `KeymapMenu.apply` syncs every menu item's accelerator from the active scheme
      (KeyChord→NSMenuItem, numpad chords left as-is) and disables items whose
      command isn't registered; Configuration ▸ Keyboard Scheme ▸ TC Classic /
      macOS Native switches schemes live (persisted, checkmarked) — so remapping is
      effective for all menu-backed commands. Runtime routing:
      `PanelListView.performKeyEquivalent` → `routeKeymap` handles modified chords
      (Ctrl/Alt/Cmd) mapped to implemented commands (bare keys/F-keys stay with the
      menu + keyDown; text fields skipped), so non-menu rebinds fire too. Interactive
      Keys page (`KeysWindowController`, Configuration ▸ Keyboard Shortcuts…):
      searchable grid, Record… (chord-capture sheet) with a displacement notice,
      Clear (suppress), Restore Defaults; edits persist to keymap-user.ini and
      re-sync the menu immediately. **T06 complete.**

## Exit checklist

- [ ] Phase B review demo (I07–I13 flows).
- [~] Inventory: F-092, F-250, F-254 done; F-251, F-252, F-253, F-255 partial; F-256 (cmdline aliases) deferred.
- [x] STATE.md updated.

## Status (2026-07-22)

I13 part 1 shipped: %-parameter engine (T02), user-command/Start-menu load+run
(T03 core), .bar parser (T04 model), and the copy-names command set (§6, F-092).
Remaining for I13 part 2: command-registry id alignment + command browser (T01,
F-250/F-255), full menu build-out + F-key relabel (T05), keymap engine + Keys
page + scheme files (T06, F-254), button-bar strip UI + Customize + drag/subbars
(rest of T04), Start-menu editor dialog + em_ from command line (rest of T03).
