# I17 — Utilities & Remaining Views

**Goal / Demo:** Files menu fully alive: change attributes, split/combine,
encode/decode, checksums, comments; duplicate finder; tree + branch view;
thumbnails view; print/export lists; occupied space.

**Prerequisites:** I16. **Read first:** SPEC-016, SPEC-008 §4.

## Tasks

- [~] T01 Change-attributes dialog (SPEC-016 §2) incl. recursive queue op +
      plugin ContentSetValue section. **AC:** perm/date matrix integration tests.
      — note: PosixPermissions model + AttributeEngine (chmod/set-date, recursive)
      + Files > Change Attributes… (octal prompt) done and tested. Remaining: the
      full rwx checkbox matrix + date picker dialog, "apply to subdirs" toggle in
      the UI, and the plugin ContentSetValue section.
- [x] T02 Split/Combine (§3) w/ .crc. **AC:** round trip incl. >4 GB sparse.
      — note: SplitInfo (.crc sidecar) + SplitCombineEngine (streamed over VFS,
      .001 parts + CRC-32 verify) + Files > Split/Combine (ByteSize.parse for
      part size). Round-trip/corrupt-part unit-tested; streaming keeps memory
      flat (huge/sparse files fine, not explicitly re-tested at >4 GB).
- [x] T03 Encode/Decode (§5). **AC:** vs system `base64`/`uuencode` goldens.
      — note: Base64 done (Base64Codec + EncodeDecodeEngine over VFS, Files >
      Encode/Decode File (Base64)); tested vs RFC 4648 vectors + round-trips.
      uuencode/xxencode deferred.
- [x] T04 Checksums (§6): create/verify UI + engine (CRC32, MD5, SHA-1/256/512;
      BLAKE3 dropped — no CryptoKit support, not worth vendoring now).
      **AC:** known test vectors; big-file streaming. — note: engine
      (ChecksumAlgorithm/Hasher, PCFoundation), .sfv + coreutils file formats
      (ChecksumFile), VFS-streaming compute/create/verify (ChecksumEngine,
      PCOperations), and Files > Create/Verify Checksum(s)… wired (SHA-256
      default). Verified vs published vectors + independent shasum. Remaining:
      algorithm chooser in the create dialog; verify-after-copy option (F-090)
      hooked into the copy engine; per-file (vs combined) output choice.
- [x] T05 Duplicate finder (SPEC-008 §4).
      **AC:** dupes fixture exactness; content-hash tiering test. — note:
      DuplicateFinder (size-tier → content-hash, PCOperations) + Files > Find
      Duplicate Files… showing dupes via ResultsFS; fixture-tested. Remaining:
      integration into the Find Files dialog UI + name-only/size-only options.
- [~] T06 Branch view Ctrl+B / Shift+Ctrl+B on ResultsFS infra + full-path
      column set. **AC:** ops from branch view hit real files. — note:
      VFSTreeWalker + Commands > Branch View / Branch View (Selected) flatten the
      tree into a ResultsFS listing (ops act on the real files via ResultsFS).
      Verified over LocalFS. Remaining: a dedicated full-path column set.
- [ ] T07 Tree panel (Ctrl+F8) + Alt+F10 tree dialog (lazy TreeModel).
      **AC:** lazy-load tests; follows panel navigation.
- [ ] T08 Thumbnails view (F-022): QLThumbnailGenerator grid + cache budget.
      **AC:** mixed-media renders; scroll perf check.
- [~] T09 Comments (§7): descript.ion engine + Ctrl+Z editor + column/view +
      Finder-comment backend option. **AC:** round trip w/ unicode + TC file fixture.
      — note: DescriptionFile (parse/generate) + CommentStore (read/set via VFS) +
      Files > Edit Comment… (Ctrl+Z) done and tested (incl. unicode/quoted names).
      Remaining: a Comment column in the panel + Finder-comment backend option.
- [~] T10 Print/export file lists + Lister print (§9); cm_CopyFileDetailsToClip
      shares generator. **AC:** generated list golden tests. — note:
      FileListFormatter (TSV/CSV/plain, golden-tested) shared by Copy Details +
      Files > Export File List… (filelist.txt) + Print File List… (NSPrintOperation).
      Remaining: Lister print, CSV/format chooser, save-panel destination.
- [~] T11 Occupied space (Ctrl+L) + volume label + system info dialogs (§1,§8).
      **AC:** unit tests on models. — note: occupied space done
      (OccupiedSpaceCalculator [PCVFS] + Files > Calculate Occupied Space…,
      unit-tested); volume-label + system-info dialogs still open.
- [x] T12 Symlink/hardlink/alias creation dialog (F-093).
      **AC:** created links verified; alias readable by Finder. — note: LinkMaker
      (symbolic/hard/alias) + Files > Create Symbolic Link/Hard Link/Alias… (name
      prompt, target = cursor item, local FS). Tested: symlink dest+read-through,
      hard-link shared inode, alias resolves. (Simple name prompt, not a combined
      dialog.)

## Exit checklist

- [ ] Inventory: F-015, F-022, F-023, F-034, F-037, F-090, F-093..F-098, F-158 done.
- [ ] STATE.md; tag `i17-done`.
