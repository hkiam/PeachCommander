# I19 — Performance Hardening, Localization, Accessibility

**Goal / Demo:** Every budget in performance.md green on the full benchmark run
(incl. 1M-entry directory); German localization complete; VoiceOver-navigable
panels; trackpad gestures; help content.

**Prerequisites:** I18. **Read first:** performance.md, test-strategy §5.

> **Status (2026-07-30): mostly done.** Localization is COMPLETE far beyond the
> original German-only goal — **19 languages** (UI catalog + full in-app Help Book).
> The Help/documentation system is live. Perf budgets validated (100k sweep);
> trackpad history + user-editable menu shipped. **Open:** accessibility (T06);
> the full 1M-dir/memory/startup sub-budgets (T02–T04) weren't separately reported.

## Tasks

- [~] T01 Full benchmark sweep (`Tools/bench.sh` finalized): run every budget,
      produce report; file a fix-task per breach (add sub-boxes here).
      **AC:** bench report committed; breaches enumerated.
      → **PARTIAL:** core budgets validated (list 100k < 1s, sort 100k < 150ms,
      filter 10k < 50ms — met with margin, PCPerfTests green); a formal committed
      bench report isn't produced.
- [~] T02 1M-dir hardening: memory profile (< 600 MB), progressive-render
      audit, collation-key memory, snapshot copy costs; incremental FSEvents
      diffing finished (no full re-list on single-file change anywhere).
      **AC:** tree-1m budgets green.
      → **PARTIAL:** incremental FSEvents diffing in place; 1M-dir memory profile
      not separately reported.
- [ ] T03 Memory-pressure behavior: cache eviction on pressure notification,
      idle RAM < 300 MB after browse spree scenario test.
      **AC:** scripted spree test green.  → **OPEN / unverified.**
- [ ] T04 Startup profile < 800 ms cold (defer non-critical init, measure with
      os_signpost). **AC:** signpost-based startup test.  → **OPEN / unverified.**
- [x] T05 Localization → **DONE, and expanded to 19 languages** (not just de):
      en, de, fr, zh-Hans, da, nl, it, ko, nb, pl, sv, sk, sl, es, cs, uk, hu, ro, ru.
      UI String Catalog `Sources/PCApp/Localizable.xcstrings` (991 keys × 19) +
      every shipping plugin + plugin menu/column/settings-pane titles. Language
      page switches. Placeholder-integrity QA clean; catalog `--validate` passes
      for all lproj. Gate: `docs/scripts/check-translations.py` (in CI).
- [ ] T06 Accessibility: identifiers audit, VoiceOver labels for panels/rows/
      status, full-keyboard-access dialog traversal, reduced-motion respect.
      **AC:** accessibility audit script (XCUI) passes.  → **OPEN** (not yet done).
- [~] T07 Trackpad gestures (F-300): two-finger swipe history (shipped + documented,
      help `macos-integration`). → **PARTIAL** — inventory F-300 = *partial*
      (swipe-history nav done; Retina/HiDPI asset audit + pinch-font not confirmed).
- [x] T08 Help → **DONE, and grown into a full documentation system.** SSOT Markdown
      in `docs/content/` generates the **Apple Help Book** (`build-helpbook.py --all`
      → `Resources/PeachCommander.help`, 19 lproj, 44 topics) and the **MkDocs site**
      (`build-site.py`). Shortcut reference generated; per-dialog/plugin topics.
      `check-docs.py` validates topics/links/images.
- [~] T09 User-editable main menu (.mnu analog, F-257) → **PARTIAL / to reconcile.**
      The command exists (`cm_ConfigMainMenu`, "Edit Menu File…") and is documented in
      help `start-menu`, **but** inventory F-257 is still `todo` — the full .mnu format
      + multiple menu files aren't confirmed complete. Docs↔code contradiction to resolve.

## Exit checklist

- [x] Language page done; help/documentation system done (19 languages).
- [ ] Accessibility (T06), formal perf sweep/memory/startup budgets (T01–T04), and
      F-300/F-257 (partial — see above) still open before an `i19-done` tag.
- [x] STATE.md updated (2026-07-30). Tag `i19-done` deferred.
