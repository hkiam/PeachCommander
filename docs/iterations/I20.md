# I20 — Release: Signing, Notarization, DMG, Sparkle, Parity Audit

**Goal / Demo:** A stapled, notarized `PeachCommander-1.0.0.dmg` that installs,
launches without Gatekeeper friction, and self-updates from the appcast to a
1.0.1 test build. Parity audit complete.

**Prerequisites:** I19 + user-provided Apple Developer ID (STATE.md open
decision — BLOCKER for T02+, do T01/T05/T06 first if missing).
**Read first:** docs/distribution/release-and-updates.md.

> **Status (2026-07-30): groundwork done; release blocked on Apple creds.** Version
> plumbing, local crash reporting, and the CI release workflow are DONE; the DMG
> builds (unsigned). **Blocked/open:** Developer-ID signing + notarization (needs
> Apple creds — `BACKLOG-OPEN.md` F-310) and Sparkle auto-update (needs feed hosting).
> Current shipping version is **0.1.0 beta**, not 1.0 — T07 parity audit + T08 RC QA
> are the path to 1.0. Note: `Tools/release.sh` referenced below does not exist; the
> real scripts are `Tools/make-dmg.sh` + `.github/workflows/release.yml`.

## Tasks

- [x] T01 Version plumbing → **DONE.** `CFBundleShortVersionString 0.1.0` +
      monotonic build number (RELEASE.md), `CHANGELOG.md` (Keep-a-Changelog),
      About/Help shows version. **AC met.**
- [ ] T02 Signing & notarization → **BLOCKED** (needs Apple Developer-ID creds).
      Groundwork done: `Resources/PeachCommander.entitlements` (hardened runtime,
      library-validation disabled for plugins), `RELEASE.md` documents the
      codesign/notarytool/staple/spctl flow. **Rest:** actually sign+notarize
      (CI currently emits an **unsigned** DMG). See F-310.
- [~] T03 DMG → **PARTIAL.** `Tools/make-dmg.sh` produces a mounting DMG (bundles
      all shipping plugins). **Rest:** notarize + `stapler validate` (blocked by T02).
- [ ] T04 Sparkle 2 auto-update → **OPEN.** Sparkle framework is vendored (see
      THIRD_PARTY_NOTICES), but the updater is **not wired** (no SUFeedURL/appcast/
      updater UI). Needs an update-feed host + EdDSA keys.
- [x] T05 Crash reporting v1 → **DONE.** On launch, newly-appeared crash reports
      are detected and — with explicit consent — revealed in Finder / copied to the
      clipboard; nothing is transmitted automatically. **AC met.**
- [x] T06 CI release workflow → **DONE.** `.github/workflows/release.yml` builds +
      tests + packages a DMG on a tag. **Rest:** the signing/notarization secrets
      (blocked by T02). README documents build/release.
- [~] T07 **Parity audit** → **PARTIAL.** Feature-inventory reconciled 2026-07-23
      (4-agent audit: 59 done · 70 partial · 43 todo · 7 n/a-macos · 2 post-1.0).
      **Rest:** a generated `docs/product/parity-report.md` and closing/justifying
      the remaining P1/P2 partials.
- [ ] T08 Release candidate QA + `v1.0.0` tag → **OPEN** (still 0.1.0 beta).

## Exit checklist

- [ ] v1.0.0 shipped — **not yet** (0.1.0 beta; blocked on Apple creds for signing/
      notarization + a Sparkle update feed).
- [x] A post-1.0 backlog exists (`BACKLOG-OPEN.md` § POST-1.0) and the open/blocked
      items are tracked there.
- [x] STATE.md updated (2026-07-30).
